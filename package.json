{
  "name": "stream-hub",
  "version": "0.0.0",
  "scripts": {
    "frontend": "cd ./frontend && ng serve",
    "backend": "cd ./backend && npm run start",
    "mongo": "docker-compose up mongo --build"
  },
  "private": true,
  "devDependencies": {
    "eslint-plugin-storybook": "^9.0.16"
  }
}
