{
  "name": "stream-hub",
  "version": "0.0.0",
  "scripts": {
    "frontend": "cd ./frontend && ng serve",
    "backend": "docker-compose up mongo backend --build",
    "app": "docker-compose up mongo backend frontend --build"
  },
  "private": true
}
